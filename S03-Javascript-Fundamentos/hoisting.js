console.log('a =', a)
var a = 2
console.log('a =', a)
// Hoisting é o mesmo que içar / jogar pra cima uma variável que ainda não foi declarada. 
// o JS considera que ela existe só não foi definida ainda.

console.log('b = ', b)
let b = 2
console.log('b =', b)
// o efeito de içamento não ocorre com let, só com var